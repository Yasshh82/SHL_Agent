GEMINI_API_KEY=your_gemini_api_key_here

# Gemini model to use (free tier options below — pick one)
# gemini-2.5-flash      ← fastest, cheapest, recommended
# gemini-2.5-flash-8b   ← even lighter, slightly lower quality
# gemini-2.5-pro        ← smarter but slower & lower free quota
GEMINI_MODEL=gemini-2.5-flash
